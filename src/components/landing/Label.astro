---
interface Props {
  accent?: boolean;
  as?: 'p' | 'span' | 'h4';
}

const { accent = false, as: Tag = 'p' } = Astro.props;
---

<Tag class:list={['cw-label', accent && 'cw-label--accent']}><slot /></Tag>
