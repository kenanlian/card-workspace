---
interface Props {
  href: string;
  primary?: boolean;
  compact?: boolean;
}

const { href, primary = false, compact = false } = Astro.props;
---

<a class:list={['cw-btn', primary && 'cw-btn--primary', compact && 'cw-btn--compact']} href={href}>
  <span><slot /></span>
</a>
