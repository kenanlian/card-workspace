---
interface Props {
  label: string;
  desc: string;
}

const { label, desc } = Astro.props;
---

<div class="cw-slot">
  <div class="cw-slot__hint">
    <span class="cw-label cw-label--accent">{label}</span>
    <span class="cw-slot__desc">{desc}</span>
  </div>
</div>
